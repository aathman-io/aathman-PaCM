version: pacm-v1

requirements:
  signature_required: true

allowed_signers:
  - name: "aathman-root"
    public_key: "<HEX_ED25519_PUBLIC_KEY>"
"

constraints:
  max_parameter_count: 5000000

